{
  "name": "tests--commerce-magento",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
      "report:merge": "npx mochawesome-merge cypress/reports/*.json > cypress/reports/mochawesome.json && npx mochawesome-report-generator -i cypress/reports/mochawesome.json -o cypress/reports/html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.12.0",
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}
